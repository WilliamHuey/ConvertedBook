// Library modules
import { BuildGenerate } from './build-generate';

export function puppeteerGenerated({
    input,
    normalizedFormats,
    flags,
    fileOutputExistence,
    checkFromServerCli,
    normalizedOutputPath
}: BuildGenerate) {

}