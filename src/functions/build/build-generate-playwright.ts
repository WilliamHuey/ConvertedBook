// Library modules
import { BuildGenerate } from './build-generate';

export function playwrightGenerated({
    input,
    normalizedFormats,
    flags,
    fileOutputExistence,
    checkFromServerCli,
    normalizedOutputPath
}: BuildGenerate) {

}